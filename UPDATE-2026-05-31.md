# Update · 2026-05-31

**TL;DR** — Shipped the **My Plan** calendar planner (`88e5cb7`). The previous
day's Golden Goblet load (`3bb121e`) is referenced below for context.

**摘要** —— 上线了「**我的排片**」日历规划器（`88e5cb7`）。前一日合并的金爵奖
竞赛单元（`3bb121e`）也一并列出作为背景说明。

---

## 1. My Plan · 我的排片 — manual calendar planner

A new top-nav pill — **My Plan** — opens a blank 10-day × 5-slot grid covering
**Jun 12–21**. Each cell starts with a `＋`. Clicking it opens a 3-step picker:

1. **Film** — search all 341 films or quick-pick from My List.
2. **Day & time** — pre-filled from the cell you clicked.
3. **Theatre** — filter by district, search across 52 venues.

Confirm and the entry drops into the slot as a chip. **Multiple film+theatre
combinations per slot are allowed** (different theatres at the same time, or
the same theatre with different films back-to-back). Toggle to **Agenda** mode
for a day-by-day list view. Every film modal also has a built-in
"Add to my plan" section. State persists across reloads via `localStorage`
(key `siff2026-plan-v2`); nothing leaves the device.

Why manual, not auto-generated: the festival hasn't published the
theatre↔timeslot↔film grid yet, so anything synthesized would be misleading.
The planner is yours to fill.

新增顶部导航「**我的排片**」（My Plan）—— 一个 **6月12日 — 6月21日** 的空白
日历网格，10天 × 5个时段。每个格子初始显示 `＋`，点击后弹出三步选择器：

1. **影片** —— 在 341 部影片中搜索，或从我的收藏快速选择。
2. **日期与时段** —— 自动带入刚点击的格子。
3. **场馆** —— 按上海各区筛选，可在 52 个影院中搜索。

确认后场次会以小卡片形式落入对应格子。**同一时段允许多个影片 + 场馆组合**
（不同影院同时段、或同一影院多部影片连场）。可切换至 **行程视图**（Agenda）
查看按日列表。在任意影片弹窗内也内嵌「加入我的排片」入口。所有数据通过
`localStorage` 本地保存（键名 `siff2026-plan-v2`），不上传任何服务器。

为何采用手动而非自动生成：电影节官方尚未公布「场馆 × 时段 × 影片」的真实排片
表，凭算法合成会误导观众。日历完全由你自己填写。

### Files

- `site/public/theatres.js` — 52 venues across Shanghai districts + 5 other
  cities, each with `flagship / imax / format` tier flags.
- `site/public/schedule.js` — date and slot primitives (5 slots: Morning 9–12,
  Midday 12–15, Afternoon 15–18, Prime 18–21, Late Night 21–24).
- `site/public/plan.jsx` — `usePlan()` hook, `<CalendarView>`,
  `<EntryPicker>`, `<FilmPlanSection>`.
- `site/public/app.jsx` — header pill, picker state, modal integration.
- `site/public/index.html` — CSS for the calendar grid, picker, agenda rows,
  per-film section.
- `site/data/SIFF_2026_Theatres.csv` — source data.

---

## 2. Golden Goblet competition · 金爵奖竞赛 (shipped 2026-05-30)

49 films across 6 categories — Main Competition, Asian New Talent,
Documentary, Animation, Live-Action Shorts, Animated Shorts — now sit at the
top of the Programme landing under a new **Competition** strand. Premiere
status (World / International / Asia) renders as an accent chip in each
film's modal.

新增 49 部影片，分布于 6 个金爵奖竞赛单元：主竞赛、亚洲新人、纪录片、动画片、
实景短片、动画短片。它们以「**Competition · 金爵奖竞赛**」为新设的最高级类目
出现在「展映单元」首页最上方。每部影片弹窗中以醒目的彩色标签显示首映级别
（全球首映 / 国际首映 / 亚洲首映）。

### Catalog impact · 片库变化

| | Before · 之前 | After · 之后 |
|---|---|---|
| Programs · 单元 | 32 | **38** |
| Films · 影片 | 292 | **341** |
| With poster · 含海报 | 261 | **281** |
| With IMDb · 含 IMDb 链接 | 247 | **264** |

### Under the hood · 实现要点

- `scripts/parse_csv.py` — added `GOBLET_PROGRAMS` dict, `split_bilingual()`
  for `<zh> (<en>)` director/country fields, `load_goblet()` hook called
  after the siff-com loop.
- `scripts/enrich_from_tmdb.py` — **bugfix**: `needs_fetch()` now respects
  `audit-erased` stubs. Without this, re-running enrichment would re-fetch
  any film with `tmdb_id: None` (including audit-erased entries) and TMDB
  would deterministically return the same wrong match, undoing prior cleanup.
- 13 cross-language TMDB mismatches caught and erased (e.g.
  *Halima* → *Halimaw Bumoto*, *Last Train* → *The Conjuring: Last Rites*).
  Audit thresholds were tightened with this pass.

`parse_csv.py` 新增金爵单元字典与 `split_bilingual()` 辅助函数（用于解析
`<中文> (<English>)` 形式的导演 / 国别字段）；`enrich_from_tmdb.py` 修复
了一个会让审计删除项「自动复活」的 bug —— 现在被审计标记为错误的条目
不会再被自动重新抓取。本次 TMDB 匹配审计中又清除了 13 处跨语言误匹配
（如 *Halima* 被误匹配为菲律宾片 *Halimaw Bumoto*；*Last Train* 被误匹配为
《招魂4：终章》）。

---

## Try it · 试用

```bash
cd site
python3 -m http.server -d public 8000
# open http://localhost:8000
```

Then: Programme → top **Competition** strand → any film modal → **Add to my
plan**. Or click the **My Plan** pill and fill the calendar directly.

打开 Programme（展映单元）—— 最上方为 Competition · 金爵奖竞赛 —— 进入任何
影片弹窗 —— 点击「Add to my plan / 加入我的排片」即可。也可直接点击右上角
**My Plan / 我的排片** 进入日历视图自行填写。

---

## What's next · 后续

- The festival hasn't released the official screening grid yet. When it does,
  `schedule.js` / `theatres.js` can stay; we'd just import a real screenings
  dataset and surface it inside each film's modal as suggested showtimes.
- Goblet TMDB matches: 6 entries are kept despite SUS flags because TMDB has
  the exact title but no release date yet (Amadeo, Boomah, The Great Skull,
  My Own Last Supper, Superbuhei, Chicken Lady). Worth a re-enrichment pass
  once TMDB fills in dates closer to the festival.

电影节官方排片表尚未发布；一旦公布，`schedule.js` / `theatres.js` 仍可保留，
只需新增真实场次数据并在影片弹窗中作为推荐场次显示即可。

金爵单元有 6 部影片虽被审计标记为 SUS，但因其在 TMDB 上已有完全匹配标题
（仅缺上映日期）而保留（Amadeo / Boomah / The Great Skull / My Own Last Supper
/ Superbuhei / Chicken Lady）。临近电影节、TMDB 补全日期后值得再跑一次匹配
脚本。
